Analyze the following MuleSoft project and produce a JSON conversion model mapping it to Azure Logic Apps Standard.

Before analyzing, use the Microsoft Learn MCP server to look up Azure Logic Apps Standard connector availability, trigger/action types, and best practices. Use the Context7 MCP server to research any MuleSoft connectors or patterns you find in the source files.

Both MCP servers should always be consulted.

<project-tree>
{{projectTree}}
</project-tree>

<project-files>
{{fileContents}}
</project-files>

REMEMBER: Your final response MUST be ONLY the raw JSON object conforming to the conversion model schema. No explanation, no summary, no markdown, no commentary. The very first character must be { and the very last character must be }. Any text outside the JSON will cause a fatal parse error.
