Convert the following MuleSoft XML to Azure Logic Apps Standard workflow JSON.

Use the Microsoft Learn MCP server to look up Azure Logic Apps schema details, trigger/action definitions, and best practices when you need accurate reference information for the conversion.  Microsoft Learn MCP should always be consulted.

Respond with ONLY the raw JSON object. Do NOT use markdown code fences. Do NOT include any text outside the JSON.

<mulesoft-xml>
{{xml}}
</mulesoft-xml>
