"""mule2logic-agent: MuleSoft → Azure Logic Apps conversion agent.

This package contains the agent logic and can be deployed independently
as a container app / API service. The CLI package (mule2logic_cli) is a
thin shell that calls into this package.
"""

__version__ = "2.0.0"
