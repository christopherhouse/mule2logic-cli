<?xml version="1.0" encoding="UTF-8"?>

<mule xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core"
      xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:wmq="http://www.mulesoft.org/schema/mule/ibm-mq"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
        http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd
        http://www.mulesoft.org/schema/mule/ibm-mq http://www.mulesoft.org/schema/mule/ibm-mq/current/mule-ibm-mq.xsd">

    <!-- HTTP Listener Config -->
    <http:listener-config name="httpListenerConfig" doc:name="HTTP Listener Config">
        <http:listener-connection host="0.0.0.0" port="8081"/>
    </http:listener-config>

    <!-- IBM MQ Config -->
    <wmq:config name="ibmMqConfig" doc:name="IBM MQ Config">
        <wmq:connection
            host="mq-hostname"
            port="1414"
            queueManager="QM1"
            channel="DEV.APP.SVRCONN"
            username="appuser"
            password="apppassword"/>
    </wmq:config>

    <flow name="http-to-ibm-mq-flow">

        <!-- Receive HTTP request -->
        <http:listener doc:name="HTTP Listener"
                       config-ref="httpListenerConfig"
                       path="/messages"
                       allowedMethods="POST"/>

        <!-- Optional: ensure the payload is JSON -->
        <ee:transform doc:name="Prepare MQ Message">
            <ee:message>
                <ee:set-payload><![CDATA[
%dw 2.0
output application/json
---
payload
                ]]></ee:set-payload>
            </ee:message>
        </ee:transform>

        <!-- Publish to IBM MQ -->
        <wmq:publish doc:name="Publish to IBM MQ"
                     config-ref="ibmMqConfig"
                     destination="QUEUE.NAME"
                     destinationType="QUEUE"/>

        <!-- Return success response -->
        <ee:transform doc:name="Build Response">
            <ee:message>
                <ee:set-payload><![CDATA[
%dw 2.0
output application/json
---
{
  status: "accepted",
  message: "Payload forwarded to IBM MQ"
}
                ]]></ee:set-payload>
            </ee:message>
        </ee:transform>

    </flow>

</mule>